# SPDX-License-Identifier: Apache-2.0

# Nim build artefacts
nimcache/
nimblecache/
htmldocs/
build/
.nim_runtime/
*.exe
*.pdb
*.ilk
*.obj
*.o
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll

# Compiled test and example binaries live next to their sources
tests/**/t[a-z]*
!tests/**/t[a-z]*.nim
examples/*
!examples/*.nim

# nimpretty scratch files produced by `nimble formatCheck`
*.nimpretty-check

# Scratch logs from running tasks locally. The repository has no legitimate
# plain-text file at its root: documentation is Markdown and LICENSE has no
# extension. Ignoring them here stops `git add -A` from capturing one.
/*.txt
/*.log

# OpenVINO artefacts that must never enter the repository or a release
# archive: runtime libraries, SDKs, plugin manifests, compiled blobs and
# model weights. Test fixtures are committed deliberately and are listed as
# exceptions by tests/fixtures/README.md.
plugins.xml
*.blob
*.bin
!tests/fixtures/*.bin
*.onnx
*.pdmodel
*.tflite
*.gguf
*.safetensors

# Editor and tooling state
.vscode/
.idea/
*.swp
*~

# OS metadata
Thumbs.db
desktop.ini
.DS_Store
